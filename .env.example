# Backend API URL (used by WaitlistModal / server-side fetches)
NEXT_PUBLIC_API_URL=http://localhost:8000/api/v1

# URL where the main dafa-ai app runs (the "Launch app" button target)
# In production: https://app.zepiai.com
NEXT_PUBLIC_APP_URL=http://localhost:5173
